# Copiar a ".env" y rellenar (el archivo .env no se sube a Git).
# Ejemplo para Docker Compose local:

OPENCAGE_API_KEY=
JWT_SECRET=sigi-municipalidad-valle-sol-secret-key-2024-segura
MYSQL_PASSWORD=root
